export function Colorbox({ color }) {
  const styles = {
    background: color,
    height: "40px",
    width: "250px",
    marginTop: "10px"
  };
  return (
    <div style={styles}>
    </div>
  );
}
